name: Docker Image CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image for e2e tests
      run: docker build . --file images/tests/Dockerfile.rhel --tag quay.io/bjarvis/ocp-tester:ocp-4.3